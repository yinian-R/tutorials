spring:
  application:
    name: config-server
  cloud:
    config:
      label: master
      server:
        git:
          uri: https://github.com/yinian-R/spring-cloud-demo.git
          search-paths: spring-cloud-config-server/src/main/resources/config

server:
  port: 8888
