## 下载 Minio
提前执行本脚本下载好文件可离线安装。若下载比较慢可使用迅雷完成下载
```
# amd64 (Intel or AMD 64-bit processors)
wget https://dl.min.io/server/minio/release/linux-amd64/minio
chmod +x minio
```

## 启动
> ./bin/run.sh start
## 停止
> ./bin/run.sh stop
## 设置开启自启动
> ./bin/system-install.sh
